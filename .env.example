# Copia este archivo a .env y rellena valores reales

# DB
CONNECTIONSTRINGS__DEFAULTCONNECTION=Host=localhost;Port=5432;Database=thrivewisdom;Username=fredy;Password=REEMPLAZA

# JWT (key ring)
JWT__ACTIVEKID=k3
JWT__KEYS__0__KID=k3
JWT__KEYS__0__KEY=REEMPLAZA_CON_SECRETO_LARGO

# SMTP (Dev)
SMTP__HOST=localhost
SMTP__PORT=1025
SMTP__USER=
SMTP__PASS=
SMTP__FROM=no-reply@thrivewisdom.local

# CORS
CORS__ALLOWEDORIGINS__0=http://localhost:5173

# ASP.NET
ASPNETCORE_ENVIRONMENT=Development
